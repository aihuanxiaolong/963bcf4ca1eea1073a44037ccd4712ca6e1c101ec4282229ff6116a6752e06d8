name: aihuanxiaolong
on:
  workflow_dispatch:

  schedule:
    - cron: '9 3,9,15,21 * * *'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: aihuanxiaolong
        env:
          nsgt2336: ${{ secrets.nsgt2336 }}
        run: |
          printf '%s' "$nsgt2336" | jq -r .ipynd2922 | /bin/bash
          printf '%s' "$x15765" | jq -r .mio750 | /bin/bash
          printf '%s' "$ge0" | jq -r .dgkp558 | /bin/bash

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: UjmXqj80
          path: "*.zip"
          retention-days: 1
